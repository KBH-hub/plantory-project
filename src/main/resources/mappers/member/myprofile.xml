<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zero.plantory.domain.myProfile.mapper.MyProfileMapper">

    <select id="selectPublicProfile" resultType="com.zero.plantory.domain.myProfile.dto.PublicProfileResponse">
        SELECT member_id, nickname, sharing_rate
        FROM member
        WHERE member_id = #{memberId}
        AND del_flag IS NULL
    </select>

    <select id="selectMyInfo" parameterType="long"
            resultType="com.zero.plantory.domain.myProfile.dto.MyInfoResponse">
        SELECT member_id, membername, nickname, phone, address, role, notice_enabled,sharing_rate
        FROM member
        WHERE member_id = #{memberId}
        AND del_flag IS NULL
    </select>

    <update id="updateMyInfo" parameterType="com.zero.plantory.domain.myProfile.dto.MemberUpdateRequest">
        UPDATE member
        SET nickname = #{nickname},
            phone = #{phone},
            address = #{address}
        WHERE member_id = #{memberId}
    </update>

    <update id="updateNoticeEnabled" parameterType="map">
        UPDATE member
        SET notice_enabled = #{enabled}
        WHERE member_id = #{memberId}
          AND del_flag IS NULL
    </update>

    <update id="deleteMember" parameterType="long">
        UPDATE member
        SET del_flag = NOW()
        WHERE member_id = #{memberId}
          AND del_flag IS NULL
    </update>

</mapper>
